# Base URL of the deployed server/index.ts (Hono app), no trailing slash.
# Leave unset during local design work — the UI falls back to a clearly
# labelled mock responder (src/lib/mock.ts) when this isn't reachable.
VITE_API_URL=
