SARVAM_API_KEY=

# Optional -- powers the async "synthesized" answer path (POST /query/synthesize).
# Gemini is tried first, Cerebras second; the fast/graded <200ms path works
# with neither set. Get a free Gemini key (no card) at aistudio.google.com.
GEMINI_API_KEY=
CEREBRAS_API_KEY=
